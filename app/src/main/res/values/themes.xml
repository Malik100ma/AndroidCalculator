<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidCalculator" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.AndroidCalculator" parent="Base.Theme.AndroidCalculator" />
<style name="CalculatorButtonActions" parent="Widget.Material3.Button">
    <item name="android:textColor">@color/black</item>
    <item name="backgroundTint">@color/blue_white</item>
</style>
    <style name="CalculatorButtonNumbers" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/black</item>
        <item name="backgroundTint">@color/white</item>
    </style>
    <style name="CalculatorButtonEquals" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/blue</item>
    </style>
</resources>